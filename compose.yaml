services:
  chat:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      PORT: 3000
    volumes:
      - chat-data:/app/data
    restart: unless-stopped
volumes:
  chat-data:
